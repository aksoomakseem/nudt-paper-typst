#import "thesis-template.typ": *
#import "templates/i-figured.typ"
#import "@preview/algo:0.3.6": algo, i, d, comment, code
#import "info.typ": *

#set heading(numbering: "1.")
#show heading: i-figured.reset-counters
#show figure: i-figured.show-figure
#show math.equation: i-figured.show-equation

#show figure: it => {
    set text(size: 10.5pt)
    it
    v(-0.5em)
    box()
}

#show math.equation: set text(font: "TeX Gyre Termes Math", size: 12pt)

#show math.equation.where(block: true): it => {
    set text(font: "TeX Gyre Termes Math", size: 12pt)
    it
    v(-1em)
    box()
}

#show figure.where(kind: table): it => [
  #set figure.caption(position: top)
  #it
]

#show cite: it => {
  set text(fill: yellow.negate(space: rgb))
  it
}
// Take a look at the file `template.typ` in the file panel
// to customize this template and discover how it works.
#show: project.with(
  sn: "philia093",
  blind-review: false,
  ..info
)

// We generated the example code below so you can see how
// your document will look. Go ahead and replace it with
// your own content!

= 绪论

== 研究背景与意义
=== 图像分类任务中的域偏移现象

在实际的图像分类应用中，训练一个高性能深度学习模型通常依赖大规模、高质量且分布一致的有标注数据集。然而，当模型部署于真实环境时，输入图像往往因获取条件、成像设备、处理方式或应用场景的差异，呈现出与训练数据截然不同的视觉特征。这种训练数据（源域）与测试数据（目标域）之间存在分布差异的现象，被称为域偏移或域漂移（Domain Shift）@Wang2018DeepVD。

#figure(image("images/1.1.png", width: 90%), 
caption: [office-home数据集@Venkateswara2017DeepHN\u{200B}中不同域的域偏移现象示意图]
)<1.1>

域偏移的来源涉及多个维度。照明条件的变化使得同一物体在室内低照度、正午强光与黄昏暖光下呈现出截然不同的颜色、亮度和对比度，尽管语义属性不变。图像风格的差异构成了另一类典型偏移：真实照片与剪贴画、油画、素描等艺术形式之间存在明显的风格鸿沟，例如@fig:1.1 所示的 Office-Home 数据集@Venkateswara2017DeepHN\u{200B}中的 Clipart 域与 Real World 域即反映出此类差异。背景环境的偏差同样不可忽视，当源域中的物体主要出现在干净的室内背景中，而目标域中的物体混入复杂室外背景时，模型极易学习到背景所引入的虚假统计相关性，而非物体本身的判别性特征@Zhu2024DebiasingVM @Ye2024SpuriousCI。成像设备与分辨率的区别亦会加剧偏移，例如不同医院采用的 CT/MRI 设备型号不同，其输出图像的噪声模式、对比度与空间分辨率各异；普通光学相机与移动端摄像头所拍摄的同一场景在纹理细节上亦不尽相同@Szumel2024TheIO。此外，数据采集方式的差异，如网络爬取图像与用户上传移动端图像在压缩伪影、水印和裁剪方式上的不一致，也会引入额外的分布漂移@Nwokeji2024SourceCI。

域偏移带来的直接后果是：在源域上训练良好的分类器，直接应用于目标域时分类准确率大幅下降。一个基于真实照片训练的“猫”分类器，在面对剪贴画风格的猫图像时往往无法正确识别，其根本原因在于模型过度依赖源域中的纹理细节与光照模式，而这些特征在目标域中已不再成立。

=== 无监督域适应的核心目标与现实价值

前文所述的域偏移现象普遍存在于各类图像分类应用中。一个直观的解决方案是在目标域上重新标注数据并微调模型。然而，在自动驾驶、医学影像等真实场景中，逐帧手工标注不仅成本高昂，甚至在某些专业领域（如罕见病诊断）因缺乏专家而不可行@Wang2018DeepVD。因此，如何在不依赖或尽可能少依赖目标域标签的前提下，使模型有效适配目标域，成为域适应研究的核心命题。

为缓解域偏移问题，域适应技术应运而生，其旨在将一个或多个源域的知识迁移至一个相关但分布不同的目标域。其中，无监督域适应（UDA, Unsupervised Domain Adaptation）因其极高的现实可行性而成为研究焦点。该设定严格遵循源域拥有完备的像素级标签，而目标域数据完全无标签的原则。其核心目标是学习一个域不变的特征表示空间，使得在此空间中，源域与目标域的同类特征分布对齐，异类特征相互分离，从而仅依赖源域标签训练出的分类器也能在目标域上取得高准确率@Wilson2018ASO。

UDA的现实价值十分重要。首先，它极大地降低了数据标注成本。在许多专业领域，获取逐像素的专家级标注耗时昂贵，而UDA使得模型可以利用已有的、易于标注的源域知识去适配海量的、无标签的目标域数据。其次，它保障了模型在非结构化环境中的泛化性，为自动驾驶、机器人视觉等需要在动态多变条件下稳定运行的系统提供了关键技术支持@Schwonberg2023SurveyOU。UDA面临的关键挑战在于，如何在没有目标域监督信号的引导下，既能有效拉近域间分布距离，又能避免在特征对齐过程中丢失目标域的判别性语义结构，防止模式崩溃或负迁移现象。

=== 多模态大模型CLIP与Mamba状态空间模型的应用潜力

面对域适应问题，近年来两种技术路径展现出变革性的潜力。首先，以CLIP为代表的多模态大模型@Radford2021LearningTV，通过在海量的图文对上进行对比预训练，习得了极为丰富的视觉-文本联合语义知识。其双塔结构将图像和文本映射到统一的512维嵌入空间，实现了强大的零样本泛化能力和跨模态对齐强度@Radford2021LearningTV。在UDA任务中，CLIP可以被视为一个强大的富语义先验基座，其文本编码器能提供对类别概念的鲁棒、非像素化的语义锚点，这为解决跨域特征对齐中的语义坍塌问题提供了新思路。然而，其核心的图像编码器（ViT）基于自注意力机制，在处理高分辨率、长序列依赖时面临平方级计算复杂度的瓶颈，限制了其对细粒度跨域特征的捕捉效率@Gu2023MambaLS。

另一方面，以Mamba为代表的结构化状态空间模型@Gu2023MambaLS\u{200B}作为序列建模的新范式，其核心优势在于线性计算复杂度和通过选择性机制实现的高效长距离依赖捕捉能力。Mamba框架通过输入依赖的参数化，在每个时间步选择性地记忆或遗忘信息，擅长从连续、序列化的数据中提取全局共享的上下文。在视觉任务中，通过将2D图像展开为patch扫描序列，Mamba有望以极高的计算效率，捕获跨域共享的序列化语义模式。因此，将Mamba的线性长程建模能力与CLIP的富语义、跨模态对齐能力相融合，在保持域不变语义的前提下，提升对长距离依赖关系和域不变特征的提取效率，构成了解决现有域适应方法“效率-语义”矛盾的一个极具潜力的突破口，也为本研究提供了核心动机。

== 国内外研究现状
=== 经典无监督域适应方法现状

无监督域适应的早期研究根植于统计分布对齐理论。该类方法的核心假设是：若能拉近源域与目标域在某个特征空间中的概率分布距离，则源域上训练的分类器可直接泛化至目标域。沿着这一思路，统计对齐方法成为经典范式之一。Sun等人@Sun2015ReturnOF @Sun2016DeepCC\u{200B}提出的CORAL通过对齐源域与目标域特征的二阶统计量协方差矩阵，以闭式解形式实现了简单高效的分布匹配。Long等人@Long2015LearningTF\u{200B}则进一步将最大均值差异引入深度网络，通过在再生核希尔伯特空间中极小化域间特征嵌入的MMD距离，使分布对齐与特征学习能够端到端联合优化。JDA@Long2013TransferFL\u{200B}等方法将边缘分布与条件分布对齐相结合，进一步提升了跨域特征的可判别性。然而，统计对齐方法通常依赖手动设计的距离度量，面对复杂、多模态的真实域偏移时，对齐能力受限。

与显式度量分布距离不同，对抗对齐方法借鉴生成对抗网络的思想，通过域判别器与特征提取器之间的极小极大博弈，隐式地实现分布对齐。Ganin@Ganin2015DomainAdversarialTO\u{200B}等人提出的DANN是该范式的里程碑工作。如@fig:1.2 所示，其在特征提取器后引入梯度反转层，使得网络在提取判别性语义的同时，生成令域分类器无法分辨的域不变特征。Tzeng等人@Tzeng2017AdversarialDD\u{200B}进一步提出ADDA，通过非对称的源域预训练与目标域对抗微调解耦学习过程。Long等人@Long2017ConditionalAD 提出的CDAN则指出，忽略类别信息而仅对齐边缘分布可能导致负迁移，进而通过将分类器预测与特征图的外积作为域判别器的多线性条件输入，实现了对条件分布的对抗对齐。尽管对抗方法极大提升了UDA的性能上限，但其训练过程对优化超参数敏感，且面临模式崩溃的风险。

#figure(image("images/1.2.png", width: 90%), 
caption: [DANN@Ganin2015DomainAdversarialTO 的对抗训练流程图]
)<1.2>

然而，这两类方法均源生于以ResNet-50等纯视觉骨干网络为主的时代，其语义表征能力受限于仅从单模态、闭合类别集合中监督学习的范式。面对开放、细粒度的域适应场景，如何引入更强的视觉-语义先验，成为突破传统方法瓶颈的关键。

=== 基于自监督与伪标签的域适应方法

随着研究的深入，学者们逐渐认识到，仅仅对齐域间分布而忽略目标域的判别性结构，极易导致类别的错误混叠。为此，以伪标签和自监督学习为核心的域适应策略成为热点。这类方法的核心在于：利用模型对目标域的高置信度预测生成伪标签，并以此作为自监督信号，迫使目标域特征形成良好的聚类结构@Li2023PseudoLF。

SHOT@Liang2020DoWR 是这一方向的代表性工作。其在源域预训练后，冻结分类器头部，仅通过信息最大化损失和伪标签自监督损失微调特征提取器，显著提升了目标域的判别性。该方法从信息论视角出发，要求模型对目标样本的预测既具有个体确定性，又具有全局多样性。3C-GAN@Li2020ModelAU 则从生成式角度出发，通过循环一致性和条件生成对抗机制，在像素级和特征级共同约束域间映射，同时利用生成的中间域样本增强伪标签的鲁棒性。此外，基于一致性正则化的方法MCC@Jin2019MinimumCC 通过最小化不同增广视角下的预测差异，进一步提升了伪标签的质量与模型的泛化性。

然而，伪标签方法存在内在的确认偏差风险。当初始伪标签存在显著噪声时，自监督训练会陷入错者恒错的恶性循环，导致模式崩溃。此外，在类别空间不对称的部分域适应场景中，基于置信度的伪标签筛选极易产生对多数类的偏向。这些挑战表明，伪标签的生成与优化需要一个更鲁棒的语义锚点来进行引导和纠偏。

=== “CLIP+域适应”的结合路线方法

多模态大模型CLIP@Radford2021LearningTV\u{200B}的问世，凭借其4亿图文对预训练所习得的强大零样本泛化能力和视觉-文本语义对齐特性，为域适应注入了全新活力。纵观近几年的相关研究，可清晰归纳出两条主要的技术改造路线：

第一条路线是提示学习参数微调。鉴于全参数微调（Full Fine-tuning）大模型的高昂计算成本及可能引发的灾难性遗忘问题，提示学习（Prompt Learning）为CLIP模型的无监督域适应提供了一条高效且独特的技术路线。该路线的核心原则是：冻结视觉编码器，仅优化文本侧上下文。其基本思想在于：CLIP强大的零样本能力源于图像与丰富文本描述的匹配，因此通过微调文本嵌入空间中分类语义的描述方式，即可调整分类权重原型，使其更好地适应下游跨域任务的视觉概念，而无需调整庞大的视觉编码器。

CoOp@Zhou2021LearningTP\u{200B}首次证明了，仅需在文本编码器前端学习少量连续的提示向量，即可在跨域识别中超越手工设计的提示模板。CoCoOp@Zhou2022ConditionalPL\u{200B}进一步通过引入轻量的元网络，动态生成与输入图像条件相关的提示，缓解了CoOp在域偏移下过拟合的问题。MaPLe@Khattak2022MaPLeMP\u{200B}则将提示学习扩展至视觉和文本双分支，在双塔的多个中间层耦合式地学习视觉提示与文本提示，实现了深层的跨模态特征调制。

#figure(image("images/1.3.png", width: 100%), 
caption: [CoOp@Zhou2021LearningTP 的提示学习流程图]
)<1.3>

然而，这类方法将域适应完全寄予提示学习，并未显式建模目标域数据的分布结构，其本质是利用CLIP语义空间的鲁棒性进行隐式对齐。

第二条路线是图像与文本特征对齐。该范式坚持经典UDA的传统，通过显式设计对齐模块来拉近CLIP多模态特征空间中的域间距离。以CLIP-Adapter@Gao2021CLIPAdapterBV\u{200B}为典型范式，通过在预训练的CLIP模型的视觉和文本分支末端添加轻量级瓶颈适配层，并利用可学习的残差比率将适配器输出的新特征与原始冻结特征进行动态混合，从而在无需复杂提示工程或全模型微调的情况下，高效地使视觉-语言模型适应下游任务，并在多项少样本分类基准上取得了优于提示调优方法的性能与效率平衡。

#figure(image("images/1.4.png", width: 100%), 
caption: [CLIP-Adapter@Gao2021CLIPAdapterBV 的特征对齐流程图]
)<1.4>

Tip-Adapter@Zhang2021TipAdapterTC\u{200B}是CLIP-Adapter的一种无需训练的非参数化扩展，Tip-Adapter通过从少样本训练集构建的键值缓存模型直接初始化权重，该权重使前向传播过程等价于从缓存中检索知识；因此，Tip-Adapter在完全无需训练的情况下即可达到与经过训练的CLIP-Adapter相媲美甚至更优的少样本分类性能，实现了效率与性能的最佳权衡。

该路线的优势在于保持了域适应任务对目标域分布进行显式建摸的特性，但其面临的关键挑战在于：如何在域对抗对齐的过程中，不破坏CLIP预训练好的精细语义边界，避免对齐操作退化为对富语义表征的语义侵蚀。

综合来看，两条路线各有所长：提示学习路线优雅高效，但缺乏对目标域分布的显式利用；特征对齐路线遵循UDA的严格设定，但在保护预训练语义流形方面困难重重。因此，如何设计一种机制，既能发挥CLIP的富语义先验，又能在显式处理域偏移的同时保护其语义完整性，是当前研究的核心前沿，也是本研究试图突破的方向。

近年来，诸如DAPrompt@Ge2022DomainAV\u{200B}等方法尝试融合前述的两条路线，不仅进行提示学习，还引入了跨域特征交互模块机制。然而，DAPrompt在将可学习向量作为提示时，忽略了提示序列内部长距离上下文的依赖建模；同时，其域对齐过程如果干预了冻结的视觉特征层，极易在早期产生伪标签噪声并破坏CLIP原本完好的视觉语义流形。这为本文引入线性复杂度的Mamba序列建模，并探索纯文本端门控交互提供了切入点。

== 主要研究内容与创新点

针对现有基于多模态大模型的域适应方法在长序列提示上下文建模与跨域特征交互稳定性方面的不足，本文以DAPrompt算法为基线框架，提出了一种Mamba网络赋能的多模态大模型无监督域适应图像分类算法。本文的主要研究内容与创新点概括如下：

+ *基于Mamba SSM的提示序列上下文建模机制* \
  现有的提示学习方法通常将提示词转化为Token后作为各自独立的可学习向量进行优化，缺乏对序列中长距离上下文依赖的结构化建模。本文创新性地引入了基于选择性状态空间模型Mamba的提示词整理模块。该模块充分利用了Mamba网络线性计算复杂度及其由输入依赖激活的选择性机制，能够针对跨域数据高效捕捉域间共享的序列化语义，使多模态大模型在避免超大计算开销的同时，获得更富连贯性的域不变提示特征表达。

+ *纯文本端跨域门控与伪标签协同优化策略* \
  在传统的跨模态特征交互中，对图像特征或图文联合特征的高频修改极易破坏CLIP预训练模型已训练完备的自然视觉网络；且面对无监督目标域时，受伪标签极高的初始噪声影响，常规门控往往导致无监督损失$"Loss"_u$快速趋零并引发模式崩溃。为此，本文摒弃了图像端的干预，提出了一种纯文本端跨域门控策略。该策略强隔离了CLIP的视觉特征编码器，仅在文本提示空间内对源域与目标域特征进行解耦与注意力门控拼装，成功规避了视觉特征的退化风险，并通过更加鲁棒的交互方式稳定了目标域泛化过程。

+ *综合统一的域适应架构与体系化实验验证* \
  本文将上述Mamba提示序列建模与纯文本门控交互机制予以有机结合，重构了一种全新的端到端无监督域适应分类训练架构。我们在Office-Home@Venkateswara2017DeepHN、DomainNet@peng2019moment\u{200B}等经典跨域基准数据集上进行了系统的实证研究。全面且定量的消融实验不仅证明了本算法在目标域准确率上稳定超越了包括原DAPrompt在内的强基线模型，更揭示了由图文联合交互转向纯文本交互在缓解特征坍塌上的实际机理。

== 论文组织结构

本文共分为六章，各章节的具体安排如下：

*第一章 绪论*：阐述无监督域适应的任务背景与研究意义，梳理基于经典对齐以及基于多模态大模型的域适应研究现状，并概括本文的研究内容与创新点。

*第二章 相关工作与理论基础*：系统介绍无监督域适应的数学形式化定义，剖析CLIP多模态大模型的双塔架构，并推导介绍Mamba状态空间模型的核心运行机制，并详细阐述DAPrompt基线方法的设计思想与训练策略，为后续改进算法奠定理论基础。

*第三章 基于优势互补的Mamba赋能提示学习算法*：深入分析多模态大模型与Mamba模型的协同互补优势，详细阐述引入Mamba提示整理模块的算法设计思路及其对上下文序列建模能力的提升。

*第四章 基于纯文本端跨域门控的伪标签优化机制*：针对原有基于图像端跨域交互的弊端，详述纯文本端门控的调整逻辑及其运算机制，展现该策略在稳定无监督损失及防止视觉特征流形破坏方面的优势，并给出总体架构网络。

*第五章 实验设计与结果分析*：介绍测试数据集、实验环境与对比基线，展示定量对比实验结果、消融实验以及深入的超参数敏感度测试及可视化分析。

*第六章 总结与展望*：总结全文核心工作与结论，指出当前研究存在的局限，并对多模态域适应的未来研究方向进行展望。

#pagebreak()

= 相关工作与理论基础

== 无监督域适应问题形式化

无监督域适应旨在将一个已标注的源域知识迁移至一个分布相关但完全未标注的目标域。其核心挑战在于克服域偏移，使模型在目标域上取得与源域相近的分类性能。本节将对该问题进行严格的符号化定义与流程抽象。

=== 源域（有标签）与目标域（无标签）设定
设源域为 $D_s = \{ (x_i^s, y_i^s) \}_{i=1}^{n_s}$，其中 $x_i^s in X_s$ 表示源域图像样本，$y_i^s in Y$ 为对应的类别标签，$Y = \{1, 2, dots, K\}$ 为共享的类别集合，且每一样本拥有明确标注。源域数据服从联合概率分布 $P_s(x, y)$。

目标域则定义为 $D_t = \{ (x_j^t) \}_{j=1}^{n_t}$，仅包含图像样本 $x_j^t in X_t$，*不提供任何标签信息*。目标域数据服从联合分布 $P_t(x, y)$，但其边缘分布与源域存在显著差异，即 $P_s(x) ≠ P_t(x)$。同时，假设条件分布亦可能发生漂移，即 $P_s(y | x) ≠ P_t(y | x)$，此现象称为协变量偏移与概念偏移的混合。

无监督域适应任务的总假设为：①源域与目标域共享同一标签空间 $Y$；②域间存在系统性分布差异，但语义类别结构一致；③训练阶段可无限制访问源域有标签数据及目标域无标签数据，测试阶段在目标域上评估。

此设定的现实对应极为广泛：源域可为干净背景的产品图集，目标域为自然场景抓拍；源域为合成渲染图像，目标域为真实照片等。

=== 域适应处理的一般化流程与数学表达

无监督域适应的通用处理流程可归纳为*特征提取-分布对齐-分类决策*的三阶段框架。设特征提取器为 $F: X → Z$，将输入图像映射至一个 $d$ 维特征空间；分类器为 $C: Z → Y$，负责输出类别预测。

模型训练的目标是最小化目标域期望风险：

$ R_t(C ∘ F) = E_{(x, y) ~ P_t} [ L ( C(F(x)), y ) ] $

由于目标域标签未知，无法直接优化。经典域适应理论给出其上界：

$ R_t (h) ≤ R_s (h) + d_(H Delta H)(D_s, D_t) + λ^* $

其中，$h = C ∘ F$，$R_s (h)$ 为源域经验风险，$d_(H Delta H)$ 衡量域间分布在假设空间中的差异，$λ^*$ 为理想联合误差。因此，无监督域适应算法通常从两方面入手：*最小化源域分类误差*与*降低域间分布差异*。

一般化数学表达中，整体优化目标可写为：

$
min_(F, C) 1/n_s sum_(i=1)^(n_s) L_"ce" ( C(F(x_i^s)), y_i^s ) + λ · d ( F(D_s), F(D_t) )
$

其中 $L_"ce"$ 为有监督交叉熵损失，$d(·, ·)$ 为所设计的域分布距离度量（如MMD、对抗损失或信息论散度），$λ$ 为平衡系数。在自监督伪标签路线中，目标域还会引入基于高置信度预测的伪标签损失 $L_"self"$，以进一步雕琢目标域特征结构。

综上，无监督域适应问题被形式化为一个有约束的分布对齐与语义迁移优化问题，其核心矛盾在于如何在缺乏目标域标签的条件下，可靠地衡量并消除域间分布差异，同时保持类别判别性。

== CLIP多模态大模型架构剖析

CLIP(Contrastive Language-Image Pre-training)是Radford等人@Radford2021LearningTV\u{200b}于2021年提出的多模态预训练范式，其以4亿图文对为训练数据，通过对比学习将视觉与语言嵌入至共享的语义空间。该模型凭借其强大的零样本泛化与跨模态对齐能力，已成为域适应研究的关键骨架。本节将从双塔结构、公共空间对齐与零样本推理三个层面进行深度解剖。

=== 双塔结构与维度投影

CLIP采用经典的双塔异构编码器架构，由图像编码器与文本编码器两支独立网络构成，两塔仅在最终投影层进行语义对齐。

图像塔（Vision Transformer, ViT）。以CLIP-ViT-B/16配置为例，输入端将原始图像裁剪至224×224像素，并以16×16的patch为单位切分为规则网格，共得到14×14=196个互不重叠的二维图像块。每个块经线性投影转换为一个768维的patch embedding向量。此外，网络在最前方追加一个可学习的[CLS]分类嵌入token，其维度同样为768维。因此，图像塔的实际输入序列长度为 197个token（196个patch token + 1个[CLS] token），每个token的维度为768。这些token添加可学习的位置编码后，依次通过12层Transformer编码器。每一层由多头自注意力和前馈网络组成，自注意力阶段所有197个token之间发生两两交互，最终输出的[CLS] token汇聚全局图像语义，形成197×768的特征矩阵，其中的[CLS]向量作为图像的全局表示进入后续投影层。该过程的计算核心为自注意力机制，其计算复杂度与序列长度的平方成正比，即$O(L^2 D)$，当需处理更细粒度的高分辨率patch时，计算开销将急剧上升。

文本塔（Text Transformer）。文本编码器基于GPT-2风格的Transformer架构，但与图像塔共享相同的12层层数。输入端采用字节对编码将原始文本（如类别描述）转换为子词单元，并在序列首尾分别添加[SOS]和[EOS]特殊标记。经过BPE分词后，文本序列被截断或填充至固定长度77个token，之后通过词嵌入层投影为768维向量（与图像塔维度匹配），并叠加可学习的位置编码。由于文本的因果属性，各层自注意力使用因果掩码，仅允许当前token关注其之前的token。最终，取[EOS]标记（即序列末尾token）处经过12层Transformer后的输出向量，作为文本的全局表示，维度为768。该表示捕获了整个文本描述的摘要语义，能够屏蔽局部顺序对全局概念的干扰。

综上，双塔分别输出形状为(197×768)的图像token序列和(77×768)的文本token序列，并以各自的聚合token（图像[CLS]、文本[EOS]）作为前往公共空间的载体。

#figure(image("images/2.1.png", width: 100%), 
caption: [CLIP@Radford2021LearningTV 的双塔架构训练与推理示意图]
)<2.1>

=== 视觉-文本公共嵌入空间对齐机制

为实现模态间的度量对齐，CLIP在双塔之后各追加一个线性投影层，将图像[CLS]向量与文本[EOS]向量分别映射至一个512维的多模态公共嵌入空间。该投影不仅完成了维度压缩，更重要的是将异质模态表征归一化到同一单位超球面上。记图像嵌入为 $bold(I) in RR^512$，文本嵌入为 $bold(T) in RR^512$，均经过L2归一化。

给定一个包含 $N$ 对图文匹配样本的batch，模型计算所有图像与文本之间的余弦相似度矩阵，形成 $N times N$ 的对数几率矩阵。训练目标为对称的InfoNCE对比损失：

$ L_"clip" = -1/(2N) sum_(i=1)^N [ log (e^(bold(I)_i^top bold(T)_i / tau)) / (sum_(j=1)^N e^(bold(I)_i^top bold(T)_j / tau)) + log (e^(bold(T)_i^top bold(I)_i / tau)) / (sum_(j=1)^N e^(bold(T)_i^top bold(I)_j / tau)) ] $

其中 $tau$ 为可学习的温度系数。该损失函数迫使匹配的图文对在512维空间中余弦相似度最大化，而不匹配的对最小化。经过海量数据训练，CLIP的公共嵌入空间呈现强大的语义对齐特性：不仅同一物体的不同视角或不同风格的图像与其对应的文本描述被拉近，甚至跨域、跨实例的图文语义也能保持高度一致。这一特性为域适应提供了天然的语义锚点——即便目标域图像的视觉风格发生了显著漂移，只要其与该类别文本描述在CLIP空间中的嵌入足够接近，即可实现无标签的准确分类。

=== CLIP的零样本推理能力解析

CLIP最具实用价值的特性之一是其零样本（Zero-shot）推理能力，即在完全未见过的数据集上直接完成图像分类，而无需任何微调。其工作流程如下：

+ 构建文本提示簇：对于目标数据集包含的$K$个候选类别，为每个类设计若干自然语言模板，如将类别“cat”扩展为“a photo of a cat”、“a sketch of a cat”等。这些模板与类别名称拼接后，送入文本编码器，生成每个类别的归一化文本嵌入 $bold(T)_k in RR^512$，共得到 $K$ 个类别原型向量。
+ 图像编码：将待分类图像送入图像编码器，提取归一化的图像嵌入 $bold(I) in RR^512$。
+ 相似度决策：计算图像嵌入与所有类别文本嵌入之间的余弦相似度，并经由一个softmax函数得到类别概率分布:

  $ P(y=k | bold(x)) = (exp(bold(I)^top bold(T)_k / tau)) / (sum_(j=1)^K exp(bold(I)^top bold(T)_j / tau)) $

  其中 $tau$ 为CLIP预训练时的温度系数。最终预测为相似度最高的类别。

这一过程的本质是将分类任务转化为图文匹配检索，完全不依赖训练过程即可泛化至任意类别空间。在域适应语境下，这一能力意味着：即使目标域数据分布显著偏移，只要其类别名称在语义上保持一致，CLIP便可在零样本条件下给出一个初步且通常不失合理性的预测。这为无监督域适应提供了高质量的初始化种子与伪标签源，也是后续CLIP+域适应方法的重要基础。

然而，零样本推理对文本模板的构造方式极为敏感，手工设计的提示往往只能捕获类别概念的均值语义，难以泛化到所有域特异的图像表达。这便为可学习的提示方法（如CoOp）以及在此基础上的域适应改进留下了广阔空间。

== 状态空间模型（Mamba）原理解析

状态空间模型作为序列建模的全新范式，凭借其线性计算复杂度和对长距离依赖的高效捕捉能力，正在深刻改变深度学习的基础架构面貌。Mamba@Gu2023MambaLS 在S4等结构化状态空间模型的基础上，通过引入输入依赖的选择性机制，成功突破了传统SSM的线性时不变约束，使得模型能够根据当前输入动态调节信息的记忆与遗忘过程。本节将从连续状态空间模型的数学基础出发，经由离散化与选择性机制，延伸至Mamba在视觉任务中的应用与计算效率分析，为后续“Mamba赋能多模态大模型”的融合设计提供坚实的理论根基。

=== 连续状态空间模型与拉普拉斯解

状态空间模型最初源于现代控制理论，用于描述动态系统从输入到输出之间的内部状态演化。给定一个连续时间的线性时不变系统，其动态行为可由如下一阶微分方程组刻画：
$
  cases(
    dot(bold(x))(t) = bold(A) bold(x)(t) + bold(B) bold(u)(t),
    bold(y)(t) = bold(C) bold(x)(t) + bold(D) bold(u)(t)
  )
$
其中，$bold(x)(t) in RR^N$ 为系统的 $N$ 维隐状态向量，$bold(u)(t) in RR^M$ 为 $M$ 维输入信号，$bold(y)(t) in RR^P$ 为 $P$ 维输出信号。矩阵 $bold(A) in RR^(N times N)$ 为状态转移矩阵，控制状态向量的自然演化；$bold(B) in RR^(N times M)$ 为输入矩阵，决定输入信号如何投射到状态空间；$bold(C) in RR^(P times N)$ 为输出矩阵，将内部状态映射为观测输出；$bold(D) in RR^(P times M)$ 为直馈矩阵，在深度学习的SSM应用中通常取零。

当输入 $bold(u)(t) = 0$ 时，系统退化为齐次方程 $dot(bold(x))(t) = bold(A) bold(x)(t)$，其解即为经典的矩阵指数形式 $bold(x)(t) = e^(bold(A) t) bold(x)(0)$。对于一般的非齐次情形，状态方程的解由齐次解与特解叠加而成：

$
  bold(x)(t) = e^(bold(A)(t - t_0)) bold(x)(t_0) + integral_(t_0)^t e^(bold(A)(t - tau)) bold(B) bold(u)(tau) dif tau
$

该表达式直观地揭示了状态空间的记忆机制：第一项 $e^(bold(A)(t - t_0)) bold(x)(t_0)$ 表示初始状态以矩阵指数的方式进行衰减式传播，第二项则表示每一个历史时刻的输入 $bold(u)(tau)$ 通过系统脉冲响应 $e^(bold(A)(t - tau)) bold(B)$ 累积至当前状态的加权和。

利用拉普拉斯变换可以得到更为简洁的频域解。对方程 $dot(bold(x))(t) = bold(A) bold(x)(t) + bold(B) bold(u)(t)$ 两端施加拉普拉斯变换，设初始状态为零（$bold(x)(0) = bold(0)$），可得：

$
  s bold(X)(s) = bold(A) bold(X)(s) + bold(B) bold(U)(s)
$

整理得到：

$
  bold(X)(s) = (s bold(I) - bold(A))^(-1) bold(B) bold(U)(s)
$

代入输出方程 $bold(Y)(s) = bold(C) bold(X)(s)$，系统的传递函数矩阵为：

$
  bold(H)(s) = frac(bold(Y)(s), bold(U)(s)) = bold(C) (s bold(I) - bold(A))^(-1) bold(B)
$

其中 $(s bold(I) - bold(A))^(-1)$ 即为预解矩阵，其极点位置决定了系统的稳定性与响应速度。通过逆拉普拉斯变换 $bold(L)^(-1) {dot}$ 可恢复时域脉冲响应，从而实现从任意输入信号到输出的完整映射。

为使状态空间模型具备对长序列的有效记忆能力，HiPPO理论为矩阵 $bold(A)$ 提供了特殊的初始化范式。HiPPO将状态向量 $bold(x)(t)$ 视作输入信号在滑动窗口上的正交多项式投影系数，通过最小化重构误差实现对历史信息的压缩表示。具体地，HiPPO-LegS采用缩放的Legendre多项式作为基函数，其导出的状态矩阵 $bold(A)$ 具备特定的负定性结构，使得历史输入的记忆权重随时间的推移以多项式速率而非指数速率衰减，从而赋予模型对长距离依赖更强的归纳偏置。

=== 零阶保持离散化与选择性机制

由于深度学习的输入数据（如图像patch序列或文本token序列）本质上是离散的，必须将连续状态空间模型转化为离散形式方可嵌入神经网络进行训练。

离散化的核心思路是通过数值积分方法在离散的采样时间点 $dots, t_k, t_(k+1), dots$ 之间建立递推关系。零阶保持是最为常用且数值稳定的离散化策略之一，其基本假设为：在每个采样间隔 $[t_k, t_(k+1)]$ 内，输入信号 $bold(u)(t)$ 保持恒定不变。设离散化步长为 $Delta = t_(k+1) - t_k$，则连续系统的离散化形式为：

$
  cases(
    bold(x)_(k+1) = accent(bold(A), macron) bold(x)_k + accent(bold(B), macron) bold(u)_k,
    bold(y)_k = bold(C) bold(x)_k
  )
$

离散化完成后，上述递推形式揭示了状态空间模型的一个核心优势：给定历史输入序列，当前输出可以依据隐状态的递推快速计算，计算复杂度为 $O(N)$，其中 $N$ 为状态维度。更为关键的是，该离散系统可展开为卷积形式。定义卷积核 $accent(bold(K), macron) = (bold(C) accent(bold(B), macron), bold(C) accent(bold(A), macron) accent(bold(B), macron), bold(C) accent(bold(A), macron)^2 accent(bold(B), macron), dots)$，则输出序列即为输入序列与该核的全局卷积 $bold(y) = accent(bold(K), macron) ast bold(u)$，这允许训练阶段通过快速傅里叶变换实现并行计算，复杂度控制在 $O(L log L)$，高度契合GPU的硬件特性。

#figure(image("images/2.2.png", width: 100%), 
caption: [状态空间模型的连续与离散形式]
)<2.2>

传统S4模型的根本局限在于参数 $bold(A)$、$bold(B)$、$bold(C)$ 和 $Delta$ 对全体输入固定不变，构成线性时不变系统。Mamba的突破性创新在于其*选择性机制*：将输入依赖的动态性引入表示空间维度，使这些核心参数成为输入的函数：

$
  bold(B)_k = f_B(bold(u)_k), quad
  bold(C)_k = f_C(bold(u)_k), quad
  Delta_k = "softplus"(f_Delta(bold(u)_k))
$

其中 $f_B, f_C, f_Delta$ 均为可学习的线性投影层，$"softplus"$ 激活函数确保时间步长始终为正。这一设计赋予Mamba在每个时间步根据当前token的语义内容自主决定记忆范围的能力：对于关键的信息性token，系统分配更大的 $Delta_k$ 以延长其影响；对于冗余的背景token，分配较小的 $Delta_k$ 使其快速被遗忘。

=== Mamba在上下文序列建模中的优势与复杂度分析

在基于多模态大模型的域适应任务中，文本端往往承担着构建类别语义原型、编码可学习提示词以及融合跨域上下文信息的核心角色。与传统Transformer自注意力机制相比，Mamba的选择性状态空间模型在文本序列建模上展现出三方面的突出优势，尤其适合处理域适应场景下的长提示序列与跨域语义整合。

+ *线性计算复杂度与长序列友好性* \
  Transformer 自注意力的计算复杂度随序列长度 $L$ 呈平方增长，即 $O(L^2 D)$，这对其处理超长提示模板或多域拼接的文本序列构成了根本性约束。Mamba的核心计算基于离散状态空间递推与并行关联扫描，其计算复杂度为 $O(L D N)$，其中 $N$ 为固定的状态维度（通常远小于 $L$）。当序列长度增长时，Mamba的训练与推理开销仅线性增加，这使得在文本端引入大规模可学习提示令牌、跨域文本特征拼接以及多源上下文融合成为可能，不会因序列膨胀而导致计算不可承受。当文本端引入更多提示令牌、拼接源域与目标域的文本特征时，这一差距将被进一步放大，从而保障了模型在有限资源下对长上下文的高效建模。

+ *选择性机制对跨域序列语义的精准筛选* \
  域适应任务中，文本序列往往混合了不同领域（源域与目标域）的语义描述、共享类别概念以及域特异的噪声信息。Mamba的选择性机制赋予每个token根据自身语义内容动态调节状态转移力度与记忆范围的能力：对于富含跨域不变语义的核心token（如类别名称、关键描述词），模型自动分配较大的时间步长 $Delta$ 以延长其影响，使其在序列远端仍被有效记忆；对于域特异的冗余或噪声 token，模型输出较小的 $Delta$ 促使其快速衰减。这一特性使得Mamba能够在不依赖显式注意力掩码的情况下，自然地从拼接的跨域文本序列中筛选出可迁移的共享语义，抑制域特异性干扰，从而为下游的伪标签生成与多模态对齐提供更纯净的文本特征表示。

+ *并行化训练与线性推理，部署友好* \
  Mamba 的离散状态空间模型在训练阶段可展开为全局卷积，通过快速傅里叶变换实现高效的并行运算，复杂度仅为 $O(L log L)$，与 Transformer 的训练效率处于同一量级。而在推理阶段，Mamba 回归递推形式，仅需维护固定大小的隐状态，每一步推理只需 $O(N)$ 的计算量，无需存储随序列长度增长的键值缓存，这显著降低了文本端在边缘部署场景下的内存开销与延迟。

综上所述，Mamba 凭借其线性复杂度、选择性上下文过滤以及推理时高效的状态递推，在文本端可学习提示与跨域序列整合任务中具备天然的建模优势。这构成了本研究选择在 CLIP 文本塔中嵌入 Mamba 模块，以提升长程跨域语义捕获与域不变特征筛选能力的重要理论依据。

== DAPrompt：基于提示学习的无监督域适应基线

前文分别阐述了无监督域适应的形式化定义、CLIP多模态大模型的架构与Mamba状态空间模型的运行机制。在进入具体的算法改进之前，有必要系统介绍本研究的基线方法——DAPrompt@Ge2022DomainAV\u{200b}。DAPrompt是将提示学习范式引入无监督域适应任务的代表性工作之一，通过在CLIP的文本端学习可优化的连续提示向量替代手工设计的离散文本模板，在冻结双塔权重的条件下实现了参数高效的跨域知识迁移。本节将从整体框架、提示学习器设计、训练策略以及优势与不足四个层面展开。

=== DAPrompt 整体框架与设计思想

DAPrompt的核心设计理念是：充分利用CLIP预训练模型蕴含的富语义先验，在*不修改图像编码器与文本编码器任何权重*的前提下，仅通过优化文本端少量可学习的连续提示向量，适配源域与目标域之间的分布差异。其整体框架由三个核心组件构成：提示学习器（PromptLearner）、冻结的CLIP双塔编码器以及基于置信度阈值的伪标签生成模块。

在DAPrompt的框架中，提示学习器为每个域-类别组合生成一条定制化的提示序列；文本编码器将这些提示映射为域特异的类别原型向量；图像编码器提取输入图像的视觉嵌入。具体而言，对于一个包含$n_d$（源域与目标域数量之和，本项目为单源域单目标域场景，通常取2）个域和$K$个类别的设定，提示学习器输出形状为$(n_d times K + K, 77, 512)$的提示张量，其中前$n_d times K$行为每个域中的每个类别对应的定制化提示，后$K$行为朴素提示（基于固定模板”a photo of a [class].”）。文本编码器将全部提示编码为512维文本嵌入后，与图像编码器输出的512维图像嵌入计算余弦相似度，得到$(n_d times K + K)$维多路logits。

这三组logits在训练中各司其职：源域提示分支（前$K$维）用于有监督分类损失；朴素提示分支（最后$K$维）基于CLIP的原始零样本知识生成目标域伪标签，提供不依赖域信息的通用判断基准；目标域提示分支（第$K$至第$2K$维）作为伪标签自监督损失的优化对象。通过这种多路并行的组织方式，DAPrompt在不引入额外对齐模块的前提下，实现了域间知识的隐式迁移。

=== 提示学习器PromptLearner设计

提示学习器是DAPrompt的核心模块，其设计遵循域共享语义＋域特定语义的解耦原则。具体而言，提示学习器维护两组可学习的连续向量：

+ *域无关上下文向量*（记为$"ctx"$）：形状为$(n_("ctx"), d)$，若启用类特定上下文CSC模式，则形状为$(K, n_("ctx"), d)$，其中$n_("ctx") = 16$为上下文token数量，$d = 512$为CLIP文本编码器的隐藏维度。该组向量编码所有域共享的通用视觉概念（如物体形状、纹理等类别本征属性），不随域变化而改变。在未启用CSC时，所有类别共享同一组$"ctx"$向量；启用CSC后，每个类别拥有独立的上下文向量以捕捉类间细粒度差异。
+ *域特定上下文向量*（记为$"dmx"$）：形状为$(n_d, n_("dmx"), d)$，其中$n_d$为域的数量，$n_("dmx") = 16$为域上下文token数量。该组向量编码每个域独特的视觉风格属性，为不同域提供差异化的语义信息。

在前向传播过程中，提示学习器将两组向量沿token维度拼接，构造完整的可学习提示序列。给定一个域$m$和类别$k$，其提示模板的构造方式为：

$ P_(m, k) = [S O S; space "ctx"^((k)); space "dmx"^((m)); space c l s_("name")^((k)); space d m_("desc")^((m)); space E O S] $

其中$"ctx"^((k))$为类别$k$对应的域无关上下文token，$"dmx"^((m))$为域$m$的域特定上下文token，$c l s_(n a m e)^((k))$为类别名称的文本嵌入，$d m_(d e s c)^((m))$为域描述文本（如, “a Real World image.”）的嵌入。这些可学习的连续token被嵌入到手工设计的提示模板中：前缀（[SOS]）与后缀（类别名＋域描述＋[EOS]）部分通过CLIP的词嵌入层预先计算并固定为buffer，不参与梯度更新；中间的可学习token块（$c t x$与$d m x$的拼接，共$n_(c t x) + n_(d m x) = 32$个token）则作为可训练参数在域适应过程中被优化。

值得指出的是，提示学习器中同时保留了一组使用固定手工模板（”a photo of a [class].”）生成的朴素提示嵌入。这组嵌入未经任何可学习token调制，完整保留了CLIP的原始零样本语义锚点，在训练中为目标域伪标签的生成提供不依赖域信息的通用判断基准。

=== 训练策略与损失函数

DAPrompt的训练过程严格遵循无监督域适应的设定：训练阶段可无限访问源域有标签数据与目标域无标签数据，测试仅在目标域上评估。训练采用双数据加载器交替迭代策略，每次迭代分别从源域和目标域各采样一个batch。

模型对源域图像的预测取前$K$维logits（对应源域提示分支），计算标准交叉熵分类损失：

$ L_x = -1/(|B_s|) sum_(i=1)^(|B_s|) log P(y_i^s | bold(x)_i^s; theta) $

对于目标域无标签样本，DAPrompt采用自训练范式。首先利用朴素提示分支的输出logits（最后$K$维），经温度系数$T$缩放后通过softmax生成伪标签概率分布：

$ bold(p)_j = s o f t m a x(f_(n a i v e)(bold(x)_j^t) / T) $

引入温度系数$T$的目的是在训练早期平滑伪标签分布，避免模型对噪声预测过度自信。仅保留最大概率超过置信度阈值$tau$的样本参与后续损失计算，以此过滤低质量伪标签：

$ hat(B)_t = {j | max(bold(p)_j) >= tau} $

目标域无监督损失基于目标域提示分支（第$K$至第$2K$维logits）计算：

$ L_u = -1/(|hat(B)_t|) sum_(j in hat(B)_t) log P(hat(y)_j^t | bold(x)_j^t; theta)_(t a r g e t) $

其中$hat(y)_j^t = a r g m a x(bold(p)_j)$为由朴素提示分支生成的伪标签。总损失为有监督损失与无监督损失的加权和：

$ L = L_x + lambda L_u $

其中$lambda$为平衡系数，控制无监督损失相对于有监督损失的权重。

值得强调的是，在整个训练过程中，CLIP的图像编码器和文本编码器的权重完全冻结，仅提示学习器中的两组可学习向量$c t x$和$d m x$接受梯度更新。这一设计使得可训练参数量仅约为$2 times 16 times 512 = 16.4 thin K$个参数（不含CSC模式），相比全参数微调的数亿参数量减少了约四个数量级，极大降低了计算开销与过拟合风险，同时有效规避了灾难性遗忘问题。

=== DAPrompt的优势与不足

综合来看，DAPrompt作为CLIP与域适应融合路线的代表性工作，具有以下几方面的显著优势：

*参数高效性*：仅优化约16K参数，可训练参数量仅为全参数微调的数万分之一，在极低的计算成本下实现了有效的领域适配。

*语义保护*：冻结CLIP双塔权重的策略有效规避了灾难性遗忘，使得CLIP预训练的精细视觉-文本语义边界在域适应过程中保持完整。朴素提示分支的保留进一步确保零样本能力不因域适配训练而退化。

*域解耦设计*：域无关上下文与域特定上下文的显式分离，使模型能够同时学习跨域不变的语义本质和域特异的风格表达，二者的独立优化赋予了框架良好的可解释性与可扩展性。

然而，DAPrompt的设计也存在两个关键不足，这构成了本研究后续改进的直接动机。

*第一，提示序列内部缺乏长距离上下文依赖的结构化建模。* 在PromptLearner中，域无关上下文$c t x$与域特定上下文$d m x$沿token维度直接拼接后送入文本编码器，各可学习token作为独立参数被优化，提示序列内部缺乏显式的上下文交互机制。这意味着模型无法显式捕获域无关语义token与域特定风格token之间的长距离依赖关系——例如，何种视觉属性在特定目标域风格下最容易被误分类。这些token之间的信息交互完全交由CLIP文本塔的自注意力层隐式完成，但由于文本塔被冻结，其自注意力权重无法根据域适应数据的特点进行调整。在多域拼接或更长提示序列的场景下，这一信息瓶颈将更为突出。这为第三章引入具有线性复杂度的Mamba选择性序列建模，以端到端方式学习提示token间的结构化上下文依赖，提供了直接的改进入口。

*第二，源域与目标域的文本特征之间缺乏显式的跨域信息交互。* DAPrompt的源域提示和目标域提示在文本编码器中独立前向传播，两个域的文本类别原型之间没有直接的信息交换通路。域适应的核心在于拉近域间分布，然而DAPrompt在文本端的跨域对齐完全隐式地依赖域共享的$c t x$向量和CLIP预训练空间的语义连续性——即假设源域与目标域猫的文本原型因共享了相同的上下文token而自然相近。这一假设在面对大跨度域偏移时可能不成立，缺少一个显式的、可控的跨域信息传递与门控机制。此外，目标域伪标签在训练初期的极高噪声，使得任何对图像特征空间的跨域操作都面临放大伪标签偏差、破坏CLIP视觉语义流形的风险。这为第四章设计纯文本端跨域门控交互器，在完全隔离视觉特征的前提下实现鲁棒的域间文本信息融合，提供了明确的改进方向。

== 本章小结

本章围绕无监督域适应图像分类的关键理论基础展开了系统性阐述，为后续算法设计提供了完备的问题定义、基线方法与模型工具支撑。

首先，从无监督域适应的形式化定义入手，明确了源域有标签、目标域全无标签的严格设定，并通过联合分布差异与期望风险上界的推导，将域适应问题抽象为”最小化源域分类误差”与”降低域间分布差异”耦合的优化目标。这一形式化为后文损失函数的设计与训练策略的选择提供了清晰的数学框架。

在此基础上，深入解剖了CLIP多模态大模型的双塔架构、对比预训练对齐机制以及零样本推理能力。通过对ViT图像塔197×768 token流与文本塔77 token序列的细粒度维度分析，揭示了512维图文公共嵌入空间作为富语义先验的独特价值。该语义空间在域适应中可提供稳定的类别语义锚点，缓解伪标签偏差与语义坍塌风险。

随后，对状态空间模型的技术脉络进行了详实推导，从连续状态方程与拉普拉斯解的频域特性，到零阶保持离散化与选择性机制的动态记忆调控，全面刻画了Mamba在序列建模中的理论优势。特别地，通过Mamba与Transformer自注意力在文本序列上的对比，论证了其$O(L D N)$线性复杂度在处理长提示序列与跨域文本拼接时的显著效率优势，以及选择性机制对跨域共享语义的自动筛选潜力。

在此基础上，系统介绍了本研究的方法基线——DAPrompt。从整体框架、提示学习器的域共享＋域特定解耦设计到基于置信度阈值的自训练损失函数，全面解析了其在参数高效性、语义保护与域解耦方面的设计优势，并深入诊断了其在提示序列长距离上下文建模缺失与跨域文本显式交互不足两方面的关键局限。这两点不足为第三章的Mamba提示序列建模改进与第四章的纯文本端跨域门控优化提供了明确的动机锚点。

综合而言，本章的工作将无监督域适应任务的形式化需求、多模态大模型的语义基础、状态空间模型的长程建模效率以及DAPrompt基线方法有机衔接，为第三章《基于优势互补的Mamba赋能提示学习域适应算法》与第四章《基于纯文本端跨域门控的伪标签优化机制》两轮迭代改进提供了坚实的理论基础与技术后效论证。

= 基于优势互补的Mamba赋能提示学习域适应算法

本章针对第二章所诊断的DAPrompt基线方法的第一项关键不足——提示序列内部缺乏长距离上下文依赖的结构化建模——展开系统性的算法改进设计。首先，通过多模态大模型与状态空间模型的能力维度结构化对照，明确二者在域适应任务中的互补区间与融合原则。在此基础上，设计了一种轻量级的Mamba风格提示序列整理模块（SelectiveStateSpaceMixer），并将其嵌入DAPrompt的提示学习器中，形成端到端的Mamba增强提示学习域适应算法。本章将从互补性分析、模块设计、提示学习器改造以及训练框架四个层面依次展开。

== 多模态大模型与状态空间模型的互补性分析

在详细展开算法设计之前，有必要从能力维度层面对CLIP多模态大模型与Mamba状态空间模型进行系统性的结构化对照，以明确“为何融合”以及“在何处融合”两个根本性问题。

=== CLIP在域适应中的能力与瓶颈

CLIP多模态大模型凭借4亿图文对的对比预训练，在域适应任务中展现出三项核心能力。

+ *语义丰度*：CLIP的双塔架构将视觉概念与自然语言描述映射至同一512维单位超球面，使得类别语义不再局限于离散的独热编码，而是以连续、稠密的文本嵌入形式存在。这一能力意味着即便目标域图像的视觉风格发生显著偏移，只要其对应的文本描述在语义上保持一致，分类边界便可借助文本锚点保持稳定。

+ *跨模态对齐强度*：CLIP的对比预训练目标直接优化图文匹配度，使得嵌入空间中猫的图像与猫的文本描述之间的距离天然近于与狗的文本描述。在域适应中，这一预对齐提供了零成本的跨模态监督信号。

+ *零样本泛化*：CLIP可在不进行任何训练的设定下对任意类别空间进行分类，为无监督域适应提供高质量的初始化预测与伪标签种子。

然而，将CLIP直接应用于域适应任务时，也存在三项与生俱来的瓶颈。

+ *自注意力平方级复杂度*：如第二章所述，ViT图像编码器中，197个token之间的全局自注意力计算复杂度为$O(L^2 D)$。当面对更高分辨率的输入或需要处理更多patch token时，计算开销急剧增长，限制了模型对细粒度跨域视觉差异的捕捉。

+ *文本塔冻结带来的适配惰性*：在DAPrompt等参数高效微调范式中，文本编码器的12层Transformer权重被完全冻结。这意味着提示token之间的长距离上下文交互完全依赖预训练阶段固化的自注意力模式，无法根据域适应数据的特点进行自适应调整。

+ *提示token的独立优化假设*：DAPrompt将$N_c + N_d = 32$个可学习提示token视为彼此独立的参数，忽略了token序列内部的顺序依赖与上下文关系，这在信息论意义上限制了提示的表达容量。

=== Mamba在跨模态场景下的潜力与局限

Mamba状态空间模型的核心优势已在第二章进行了详实推导，此处从跨模态域适应的视角进行重新审视优势所在。

+ *线性复杂度长序列建模*：Mamba的$O(L D N)$线性计算复杂度使得引入更多提示token、拼接更长跨域文本序列成为可能，不会因序列膨胀而导致计算不可承受。

+ *选择性信息筛选*：Mamba的选择性机制赋予每个token根据自身语义内容动态调节状态转移力度与记忆范围的能力——关键语义token获得较大时间步长$Delta$以延长影响，冗余token则快速衰减。这一特性天然适配跨域提示序列中域不变类别语义与域特定风格描述的自动解耦与筛选。

+ *高效推理部署*：Mamba在推理阶段回归递推形式，仅需维护固定大小的隐状态，在边缘部署场景下具有显著优势。

然而，Mamba在跨模态域适应场景下的应用仍存在尚未充分验证的局限。

+ *视觉2D结构适配不足*：Mamba最初为1D序列建模设计，虽然Vision Mamba等后续工作通过多种扫描策略将2D图像展开为1D序列，但在域适应的跨模态场景中，Mamba的最优嵌入位置与作用形式尚缺乏系统研究。

+ *跨模态语义理解缺失*：与CLIP不同，Mamba本身不具备视觉-文本跨模态对齐能力，无法独立提供富语义先验。其角色定位应当是对CLIP语义空间的增强与补充，而非替代。

+ *选择性机制在短序列上的收益存疑*：Mamba的选择性机制在数千token以上的长序列上展现出显著优势，而在域适应中提示序列通常仅包含数十个token。如何在短序列条件下释放选择性机制的能力，是设计中的核心挑战。

=== 能力维度结构化对照与互补区间定位

为系统性地定位CLIP与Mamba在域适应任务中的互补关系，@tbl:3.1 从语义丰度、计算效率、序列友好性、跨模态对齐强度和域鲁棒性五个维度进行了结构化对照。

#figure(
  table(
    columns: 3,
    stroke: (x: none, y: 0.5pt),
    align: (left, center, center),
    table.header(
      [*能力维度*], [*CLIP多模态大模型*], [*Mamba状态空间模型*]
    ),
    [语义丰度], [极高：4亿图文对预训练，连续语义空间], [无独立语义理解能力，依赖外部输入嵌入],
    [计算效率], [$O(L^2 D)$，长序列下计算瓶颈显著], [$O(L D N)$，长序列下效率优势突出],
    [序列友好性], [自注意力全局交互，但平方复杂度限制序列扩展], [线性递推，天然支持长序列与多源拼接],
    [跨模态对齐强度], [极强：对比学习显式优化图文匹配], [无内置跨模态对齐，需借助外部多模态骨架],
    [域鲁棒性], [零样本泛化强，但对细粒度域差异的捕捉依赖数据规模], [选择性机制可动态筛选域不变语义，域噪声抑制潜力大],
  ),
  caption: [CLIP与Mamba在域适应任务中的能力维度结构化对照]
)<3.1>

从@tbl:3.1 可以清晰地识别出三组互补区间。

其一是语义-效率互补。CLIP提供富语义先验但受限于平方复杂度，Mamba具备线性效率但缺乏语义理解——二者在语义质量与计算效率维度上形成天然互补。

其二是对齐-筛选互补。CLIP擅长跨模态对齐但难以自适应筛选域不变特征，Mamba的选择性机制恰好可以在CLIP对齐后的语义空间中进一步筛选跨域共享语义、抑制域特异性噪声。

其三是全局-局部互补。CLIP的自注意力提供全局token交互但代价高昂，Mamba的局部卷积与选择性状态递推可以以极低代价增强提示序列内部的局部上下文混合。

=== Mamba赋能多模态大模型的融合原则

基于上述互补性分析，本文提出Mamba赋能多模态大模型的三项融合原则，以指导后续的算法设计。

*原则一：语义主次分明。* CLIP的预训练语义空间是域适应任务的核心，提供类别语义原型、跨模态对齐和零样本泛化能力。Mamba的功能定位是辅助增强，在CLIP语义空间内部优化提示序列的上下文表示，而非重新建模语义或替代CLIP的任何组件。这一原则要求Mamba模块的嵌入不得破坏CLIP已训练完备的视觉-文本语义流形。

*原则二：文本端优先嵌入。* 在域适应任务中，图像编码器的输出特征直接参与域对齐和分类决策，对语义完整性的要求极高。将Mamba嵌入文本端而非图像端，可以充分利用Mamba的1D序列建模专长，同时规避对2D视觉结构适配的不确定性。此外，文本端嵌入可以共享CLIP文本塔的可学习位置编码和层归一化参数，实现更紧密的语义耦合。

*原则三：残差融合与轻量化。* Mamba模块应以残差连接的方式嵌入现有架构，确保在如残差权重趋零等极端情况下模型可退化为原始DAPrompt基线，保障训练稳定性。同时，Mamba模块的设计应保持轻量化。本文的SelectiveStateSpaceMixer仅增加约1.5M可训练参数，相比CLIP ViT-B/16的86M参数量可忽略不计。

上述三项原则共同构成了Mamba赋能的设计纲领：*在保持CLIP语义一致性的前提下，以残差、轻量的方式在文本端引入选择性状态空间建模，提升提示序列的长程依赖建模效率与域不变特征提取能力*。

== Mamba风格提示序列整理模块设计

=== 问题定义：提示序列的上下文建模缺失

回顾第二章§2.4中对DAPrompt基线不足的诊断，其提示学习器将域无关上下文向量（记为$C$，对应代码中的ctx）与域特定上下文向量（记为$D$，对应代码中的dmx）沿token维度直接拼接，构造提示序列：

$ P = [S O S; space C; space D; space c l s; space d o m; space E O S] $

在此设计中，$C$中的$N_c$个token与$D$中的$N_d$个token各自作为独立可学习参数被优化，token之间缺乏显式的结构化交互。CLIP文本编码器虽然通过自注意力机制实现了token间的全局交互，但其权重在域适应训练中被完全冻结——这意味着自注意力矩阵是预训练阶段固化下来的，无法感知域适应数据的特定分布特征。由此形成了一个核心矛盾：可学习的token参数需要上下文交互来充分表达跨域语义，但实现上下文交互的自注意力层又不可调整。

解决这一矛盾的思路是：在提示token进入冻结的CLIP文本塔之前，插入一个轻量级的、可训练的序列建模模块，对提示序列内部的token依赖关系进行预整理。

=== SelectiveStateSpaceMixer模块结构设计

基于上述分析，本文设计了SelectiveStateSpaceMixer，一个轻量级的Mamba风格序列混合模块，其结构如//@fig:3.1 
所示。

//#figure(image("images/3.1.png", width: 90%),
//caption: [SelectiveStateSpaceMixer模块结构示意图]
//)<3.1>

该模块由以下组件构成：

+ *输入投影与门控分支*：输入序列$x in RR^(L times d)$分别通过两个线性投影层，其中输入投影$W_("in") in RR^(d times d_h)$将各token映射至扩展维度$d_h = 2d$，经SiLU激活后得到特征$u$；门控投影$W_("gate") in RR^(d times d_h)$经Sigmoid激活后得到逐元素门控信号$g$，控制后续混合信息的通过比例。
+ *深度可分离1D卷积*：对$u$沿序列维度施加分组数等于$d_h$的深度可分离卷积（卷积核大小为3，因果填充），实现轻量级的局部上下文混合。卷积核在训练中可学习，能够自适应地聚合相邻token的语义信息。
+ *选择性状态参数生成*：卷积输出经两个独立的线性投影$W_a in RR^(d_h times d_h)$和$W_b in RR^(d_h times d_h)$，分别经Sigmoid激活后生成选择性参数$a_t$和$b_t$。其中$a_t$控制历史状态的遗忘/保留比例，$b_t$为当前输入贡献的候选状态更新量。这一设计与Mamba中输入依赖的$Delta$、$B$、$C$参数化理念一致，但针对短序列场景做了简化。
+ *选择性扫描与状态递推*：沿序列维度执行前向扫描，在第$t$个时间步，隐状态按如下规则更新：

  $ h_t = a_t ⊙ h_(t-1) + (1 - a_t) ⊙ b_t $

  其中$⊙$表示逐元素乘法。当$a_t arrow.r 1$时，模型保留历史信息；当$a_t arrow.r 0$时，模型用当前输入状态$b_t$覆盖历史信息。由于$a_t$依赖输入$x_t$，模型可以根据每个token的语义内容自主决定记忆或遗忘，这正是Mamba选择性机制在提示序列上的核心体现。
+ *输出投影与残差连接*：扫描输出$y$经门控$g$调制后，通过输出投影$W_("out") in RR^(d_h times d)$恢复至原始维度$d$。最终输出以残差形式与原始输入相加，缩放因子$alpha$为可学习参数经Sigmoid激活后的标量：

  $ x' = x + sigma(alpha) dot "SSM"(x) $

整个模块的可训练参数量约为$4 d d_h + k d_h + 2 d_h^2 + d_h d + 1$项，取$d = 512$、$d_h = 1024$、$k = 3$，约合3.6M参数。在域适应的典型训练设定（50 epochs × 数千iterations）下，这一参数增量带来的计算开销几乎可忽略。

=== 选择性扫描在提示序列上的作用机理

值得深入分析的是，选择性扫描机制在处理提示序列时究竟发挥了何种作用。提示序列由两部分组成：域无关上下文token编码类别本征属性，域特定上下文token编码域风格属性。这两类token在序列中交替排布，选择性扫描机制通过输入依赖的$a_t$参数，可以自适应地调节两类token之间的信息流动。

具体而言，对于域特定上下文token，如果其编码的风格信息在跨域迁移中属于冗余噪声（例如强光造成的过曝信息在室内场景中无意义），模型可以学习为这些token分配较小的$a_t$值，使其对后续域无关token的影响快速衰减。反之，对于域无关上下文token中的核心语义信息，模型分配较大的$a_t$值，使其影响贯穿整个序列，成为提示序列中的长程语义锚点。这种机制在不依赖显式注意力掩码的情况下，实现了对跨域语义的软性筛选，为后续的文本编码器提供了更纯净、更结构化的提示表示。

== 基于Mamba增强的提示学习器

=== PromptLearnerMamba的整体设计

在SelectiveStateSpaceMixer的基础上，本节将其嵌入DAPrompt的提示学习器中，构造增强型提示学习器PromptLearnerMamba。其设计遵循§3.1.4中提出的三项融合原则：Mamba模块以残差方式嵌入文本端提示空间，保持CLIP视觉编码器完全冻结，且所有原始DAPrompt的可学习组件（$c t x$和$d m x$）保留不变。

具体而言，PromptLearnerMamba继承自PromptLearner，在前向传播过程中新增了一个关键步骤：在域无关上下文与域特定上下文沿token维度拼接之后、与固定的前后缀（SOS/EOS/类别名/域描述嵌入）拼接之前，将拼接后的提示token块送入SelectiveStateSpaceMixer进行序列混合。混合后的token与原始token通过可学习的残差缩放因子进行融合。整个过程的伪代码如@alg:3.1 所示。
#figure(
  kind: "algorithm",
  supplement: [算法],
  numbering: "3.1",
  caption: [PromptLearnerMamba 前向传播伪代码],
)[
  #algo(
    title: [
      #set text(size: 15pt)
      *算法3-1*:#emph(smallcaps("PromptLearnerMamba"))
    ],
    parameters: ([#math.italic("C, D, prefix, suffix, naive")],),
    strong-keywords: true,
    keywords: ("if", "else", "then", "while", "for", "repeat", "do", "until", ":", "end", "and", "or", "not", "in", "to", "down", "let", "return", "goto"),
    indent-size: 15pt,
    indent-guides: 1pt + gray,
    row-gutter: 10pt,
    column-gutter: 10pt,
    inset: 8pt,
    stroke: (
    top: 1.5pt + black,
    bottom: 1.5pt + black,
    left: none,
    right: none,
    ),
    fill: none,
  )[
    // 阶段一：上下文向量维度展开\
    $bold("if")$ $"CSC"$#i \
    $C arrow.l C."unsqueeze"(0)."expand"(M, minus 1, minus 1, minus 1)$#d #comment[$[K,N_c,d] arrow [M,K,N_c,d]$]\
    $bold("else")$ #i \
    $C arrow.l C."unsqueeze"(0)."expand"(M, minus 1, minus 1)$#d\
    $C arrow.l C."unsqueeze"(1)."expand"(minus 1, K, minus 1, minus 1)$ #comment[$[N_c,d] arrow [M,K,N_c,d]$]\
    $D arrow.l D."unsqueeze"(1)."expand"(minus 1, K, minus 1, minus 1)$ \
    // 阶段二：提示token块拼接与展平\
    $T arrow.l "concat"(C, D, "dim" = 2)$ #comment[$[M,K,N_c+N_d,d]$]\
    $T arrow.l "reshape"(T, [M dot K, space N_c+N_d, space d])$ #comment[$[M times K,N_c+N_d,d]$]\
    // 阶段三：选择性状态空间序列混合\
    $u arrow.l "SiLU"("Linear"_"in"(T))$ #comment[$[M times K,N_c+N_d,2d]$] \
    $g arrow.l sigma("Linear"_"gate"(T))$ #comment[门控信号]\
    $u arrow.l "DepthwiseConv1D"(u, space "kernel" = 3)$ \
    $a arrow.l sigma("Linear"_a(u))$ #comment[选择性遗忘门]\
    $b arrow.l "Linear"_b(u)$ #comment[选择性遗忘状态]\
    $h arrow.l 0$ #comment[初始化隐状态]\
    $bold("for")$ $t arrow.l 1$ $bold("to")$ $L$ $bold("do")$#i \
    $h arrow.l a_(":,t") ⊙ h + (1 minus a_(":,t")) ⊙ b_(":,t")$#d \
    $T_"mix" arrow.l "Linear"_"out"(g ⊙ "stack"(h_1, dots, h_L))$ \
    // 阶段四：残差融合与提示模板装配\
    $alpha arrow.l sigma(s)$ \
    $T arrow.l T + alpha dot T_"mix"$ #comment[残差连接]\
    $P arrow.l "concat"(p r e f i x, T, s u f f i x, space "dim" = 1)$ #comment[$[M K, 77, d]$]\
    $P arrow.l "concat"(P, n a i v e, space "dim" = 0)$ #comment[$[M K + K, 77, d]$]\
    return $P$ #comment($P in RR^(M K + K times 77 times d)$)\
  ]
]<alg:3.1>



=== 前向传播流程与维度变化分析

PromptLearnerMamba的前向传播涉及多步维度变换，此处在@alg:3.1 的基础上对关键步骤的维度变化进行详细分析。

*步骤1-6：上下文向量的域/类别维度展开。* 域无关上下文$C$的初始形状取决于是否启用类特定上下文（CSC）模式。若未启用CSC，$C$的形状为$(1, N_c, d)$，扩展为$(M, K, N_c, d)$；若启用CSC，$C$的形状为$(K, N_c, d)$，扩展为$(M, K, N_c, d)$。域特定上下文$D$的初始形状为$(M, N_d, d)$，经步骤6扩展为$(M, K, N_d, d)$。

*步骤7-8：提示token块的拼接与展平。* 两类上下文沿第3维（token维度）拼接后得到形状为$(M, K, N_c + N_d, d)$的张量，即$M times K$条提示序列，每条序列包含$N_c + N_d = 32$个可学习token。经步骤8展平为$(M K, N_c + N_d, d)$，以batch维度并行送入SelectiveStateSpaceMixer。

*步骤9-19：Mamba序列混合与残差融合。* SelectiveStateSpaceMixer在前$M K$条提示序列上独立进行选择性扫描。每个序列的32个token沿序列维度依次递推，隐状态维度的信息在token间流动。混合后的张量$T_("mix")$形状仍为$(M K, N_c + N_d, d)$。残差融合因子$sigma(alpha) in (0, 1)$控制Mamba混合信号在最终提示表示中的占比，训练初期$alpha$初始化为较小值（默认为0.2），使模型从接近原始DAPrompt的状态开始学习，逐步引入序列混合能力。

*步骤20-22：提示模板装配。* 混合后的token块与固定的prefix（SOS token嵌入，形状$(M K, 1, d)$）、suffix（类别名＋域描述＋EOS嵌入，形状$(M K, 77 - (N_c + N_d) - 1, d)$）沿序列维度拼接，得到形状为$(M K, 77, d)$的域感知提示集。随后追加以固定手工模板生成的朴素提示$(K, 77, d)$，最终输出形状为$(M K + K, 77, d)$的完整提示张量。此输出随后被送入冻结的CLIP文本编码器，生成文本特征嵌入。

值得指出的是，SelectiveStateSpaceMixer对$M K$条提示序列是*并行且独立*处理的——即混合仅发生在每条序列内部的32个token之间，序列之间（不同域-类别组合之间）不存在token级的信息交互。这一设计确保了跨域信息交换在后续的跨模态对比阶段（图像嵌入与文本嵌入的余弦相似度计算）隐式完成，避免了对提示空间结构的过度干预。

== 训练框架与算法流程

=== 整体模型架构

将PromptLearnerMamba封装后，形成完整的Mamba增强域适应模型CustomCLIPMamba，其架构如//@fig:3.2 
所示。该模型沿袭DAPrompt的整体数据流，核心变化在于提示学习器部分引入了Mamba序列混合能力。

//#figure(image("images/3.2.png", width: 100%),
//caption: [基于Mamba增强的域适应模型（CustomCLIPMamba）整体架构图]
//)<3.2>

模型的前向传播分为三个阶段。

*提示编码阶段*：PromptLearnerMamba生成Mamba增强的提示张量$(M K + K, 77, d)$，经冻结的CLIP文本塔编码为文本特征嵌入$T in RR^((M K + K) times d_t)$，其中$d_t = 512$为CLIP投影空间维度。

*图像编码阶段*：源域和目标域图像分别经冻结的CLIP ViT编码为图像特征嵌入$I_s, I_t in RR^(B times d_t)$。

*多路分类阶段*：图像嵌入与文本嵌入计算余弦相似度并经温度系数放缩后，得到多路logits——源域提示分支（前$K$维）用于有监督损失，目标域提示分支（第$K$至$2K$维）用于无监督损失，朴素提示分支（最后$K$维）用于伪标签生成。

=== 训练算法伪代码

完整训练流程的伪代码如@alg:3.2 所示。训练过程沿袭DAPrompt的双数据加载器交替迭代策略和基于置信度阈值的自训练范式，损失函数保持不变。

#figure(
  kind: "algorithm",
  supplement: [算法],
  caption: [DAPL_MAMBA 完整训练流程伪代码。训练过程中仅提示学习器参数 $theta$ 接受梯度更新，CLIP双塔权重保持冻结。],
)[
  #algo(
    title: [
      #set text(size: 14pt)
      *算法3-2*:#emph(smallcaps("DAPL_MAMBA"))
    ],
    parameters: ([#math.italic("D_s,D_t, E, tau, T, lambda")],),
    strong-keywords: true,
    keywords: ("if", "else", "then", "while", "for", "repeat", "do", "until", ":", "end", "and", "or", "not", "in", "to", "down", "let", "return", "goto"),
    indent-size: 15pt,
    indent-guides: 1pt + gray,
    row-gutter: 10pt,
    column-gutter: 10pt,
    inset: 8pt,
    stroke: (
    top: 1.5pt + black,
    bottom: 1.5pt + black,
    left: none,
    right: none,
    ),
    fill: none,
  )[
    // 初始化\
    $"Init"("PromptLearnerMamba", "CLIP")$ \
    // 训练主循环\
    $bold("for")$ $e arrow.l 1$ $bold("to")$ $E$ $bold("do")$#i \
    $bold("for")$ $"each"$ $(B_s, B_u)$ $bold("in")$ $(D_s, D_t)$ $bold("do")$#i \
    // 阶段一：前向传播\
    $P arrow.l "PromptLearnerMamba"()$ #comment[$P in RR^(M K + K times 77 times d)$]\
    $T arrow.l "TextEncoder"(P)$ #comment[冻结文本塔]\
    $I_s arrow.l "ImageEncoder"(B_s)$ #comment[冻结ViT，$I_s in RR^(|B_s| times 512)$]\
    $I_u arrow.l "ImageEncoder"(B_u)$ #comment[冻结ViT，$I_u in RR^(|B_u| times 512)$]\
    $L_s arrow.l I_s T^top$ #comment[源域logits]\
    $L_u arrow.l I_u T^top$ #comment[目标域logits]\
    // 阶段二：伪标签生成\
    $p arrow.l "softmax"(L_(u,:,-K:) \/ T)$ #comment[朴素提示分支，温度$T$缩放]\
    $hat(y), space c arrow.l "argmax"(p), space "max"(p)$ #comment[伪标签$hat(y)$与置信度$c$]\
    $"mask" arrow.l (c >= tau)$ #comment[置信度阈值$tau$]\
    // 阶段三：损失计算\
    $L_x arrow.l "CrossEntropy"(L_(s,:,:K), space y^s)$ #comment[源域有监督交叉熵损失]\
    $L_u arrow.l "masked_CE"(L_(u,:,K:2K), space hat(y), space "mask")$ #comment[目标域伪标签损失]\
    $L arrow.l L_x + lambda L_u$ #comment[总损失，$lambda$为无监督权重]\
    // 阶段四：反向传播与参数更新\
    $L."backward"()$ #comment[反向传播]\
    $"optimizer.step"()$ #comment[更新PromptLearnerMamba]\
    $bold("end")$#d \
    $bold("end")$#d \
    // 测试评估（每个epoch结束后）\
    $bold("if")$ $"eval"$ $bold("then")$#i \
    $"Test"(theta)$ #comment[在目标域测试集评估]\
    $bold("end")$#d \
  ]
]<alg:3.2>

=== 与DAPrompt基线的关键差异

在结束本章之前，有必要将所提出的Mamba增强算法与DAPrompt基线进行系统性的差异对比，以明确改进的实质内容。

*架构层面*：唯一的差异在于提示学习器的前向传播中插入了SelectiveStateSpaceMixer。PromptLearnerMamba对拼接后的提示token块进行Mamba选择性扫描与残差融合，而原始PromptLearner直接将拼接后的token块送入模板装配。除此之外，图像编码器、文本编码器、提示模板结构、多路logits的组织方式以及朴素提示分支均与DAPrompt完全一致。

*训练层面*：损失函数（$L = L_x + lambda L_u$）、伪标签生成机制（朴素提示分支＋温度缩放＋置信度阈值筛选）、双数据加载器交替迭代策略以及优化器配置均与DAPrompt保持一致。这意味着本文的改进是*可插拔式的增量增强*：在不改变训练流程的前提下，仅通过替换提示学习器即可获得性能提升。

*参数层面*：SelectiveStateSpaceMixer引入了约3.6M额外可训练参数，相比原始DAPrompt的约16K参数增加了两个数量级。但需要注意的是：第一，3.6M参数仅占CLIP ViT-B/16模型总量（约86M）的4.2%，仍属于参数高效微调的范畴；第二，这些新增参数集中在提示学习器内部，CLIP双塔的权重保持完全冻结，因此不会引入灾难性遗忘风险。

== 本章小结

本章针对DAPrompt基线方法中提示序列缺乏长距离上下文依赖建模的问题，提出了基于Mamba状态空间模型的增量式改进方案。

首先，通过CLIP与Mamba在语义丰度、计算效率、序列友好性、跨模态对齐强度和域鲁棒性五个维度的结构化对照，明确了二者在域适应任务中的语义-效率、对齐-筛选、全局-局部三组互补区间，并据此提出了Mamba赋能多模态大模型的三项融合原则：语义主次分明、文本端优先嵌入、残差融合与轻量化。

在此基础上，设计了SelectiveStateSpaceMixer，一个轻量级的Mamba风格序列混合模块。该模块通过输入投影与门控双分支、深度可分离1D卷积局部混合以及选择性状态扫描机制，在保持$O(L d_h)$线性复杂度的前提下，赋予提示序列内部token之间显式的、可学习的上下文依赖建模能力。选择性扫描机制能够自适应地调节域无关语义token与域特定风格token之间的信息流动，实现跨域语义的软性筛选。

随后，将SelectiveStateSpaceMixer以残差方式嵌入DAPrompt的提示学习器中，构造了PromptLearnerMamba。该增强型提示学习器在保持DAPrompt全部原始组件和训练策略不变的前提下，通过在前向传播中增加一步Mamba序列混合操作，赋予了模型端到端学习提示token间结构化上下文依赖的能力。

本章的改进是可插拔式的增量增强，损失函数、伪标签生成机制和训练流程与DAPrompt基线完全一致，确保了消融实验中改进归因的清晰性。然而，本章的方法仍未解决DAPrompt的第二项关键不足：源域与目标域文本特征之间缺乏显式的跨域信息交互，这将是第四章的核心议题。
